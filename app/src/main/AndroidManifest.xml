<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.lidachui.s4_demo">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.S4_Demo"
        tools:targetApi="31"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".TaskManagerActivity"
            android:exported="true">

        </activity>
        <activity android:name=".BaseActivity" />
        <activity android:name=".AddTaskActivity" />
        <activity android:name=".HandVehActivity" />
        <activity android:name=".ViewMyTaskActivity" />
        <activity android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TransportationStatisticsActivity" />
        <activity android:name=".TravelRecordActivity" />
    </application>

</manifest>